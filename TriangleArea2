const valorBase = prompt('Escriba la base del triángulo: ');
const valorAltura = prompt('Escriba la altura del triángulo: ');

// calculate the area
const valorÁrea = (valorBase * valorAltura) / 2;

console.log(
  `El area del triángulo es: ${areaValue}`
);